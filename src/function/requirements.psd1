# This file lists PowerShell modules that are automatically installed
# when the Function App starts. We intentionally keep this empty —
# all operations use raw REST API calls for maximum reliability.

@{
    # No external module dependencies
}
